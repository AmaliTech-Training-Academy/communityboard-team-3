# Test Environment Configuration
# Used for running tests in isolation

# Database
POSTGRES_DB=communityboard_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=test_password_12345
POSTGRES_PORT=5433

# Backend
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5433/communityboard_test
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=test_password_12345
JWT_SECRET=test-secret-key-do-not-use-in-prod
SPRING_PROFILES_ACTIVE=test

# Frontend
VITE_API_BASE_URL=http://localhost:8081

# Ports (different from dev to avoid conflicts)
BACKEND_PORT=8081
FRONTEND_PORT=3001
