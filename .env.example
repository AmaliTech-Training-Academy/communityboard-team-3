# Source database (operational)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=communityboard
DB_USER=postgres
DB_PASSWORD=changeme

# Analytics database (ETL output — separate instance)
ANALYTICS_DB_HOST=localhost
ANALYTICS_DB_PORT=5434
ANALYTICS_DB_NAME=communityboard_analytics
ANALYTICS_DB_USER=postgres
ANALYTICS_DB_PASSWORD=changeme

# Backend
JWT_SECRET=change-this-to-a-long-random-string

# ETL encryption — generate with: python -c "import secrets; print(secrets.token_hex(32))"
ETL_KMS_KEY=
